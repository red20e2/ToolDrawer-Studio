# V0.1 Release Hardening Plan Review Amendments

These items are normative additions to `2026-08-13-v0.1-release-hardening.md` and close gaps found during the required plan self-review.

## Amendment A: Source-vs-Frozen Resource Paths

Extend Task 2:
- `src/tooldrawer_studio/app_paths.py` also provides `resource_root() -> Path` and `resource_path(*parts: str) -> Path`.
- Source mode resolves from the repository/package location.
- Frozen mode resolves from PyInstaller's runtime bundle location (`sys._MEIPASS` when present) without changing `%LOCALAPPDATA%` settings/log locations.
- `tests/unit/test_app_paths.py` must cover source and simulated frozen resolution.

## Amendment B: Camera and Network Resilience

Insert this task before the release fixture/package tasks.

### Task 5B: Optional Capture Feature Resilience

**Files:** Modify `src/tooldrawer_studio/capture/webcam.py`, `src/tooldrawer_studio/capture/phone_server.py`, `src/tooldrawer_studio/ui/main_window.py`; create `tests/unit/test_capture_resilience.py`.

**Produces:** camera enumeration/open and phone-session startup failures remain recoverable feature errors; app startup and imported-photo workflow remain usable.

- [ ] Write failing tests that inject: no cameras, camera-open failure, private-LAN-address discovery failure, phone-server bind failure, and network-address disappearance after session start.
- [ ] Assert `MainWindow()` still constructs and Import Photo remains usable in every injected failure case; no failure may escape application startup.
- [ ] Run `python -m pytest -q tests/unit/test_capture_resilience.py`; expect red where current behavior is not resilient.
- [ ] Implement narrow exception boundaries around camera enumeration/open and phone-session start/status refresh. Keep existing security behavior: phone capture still fails closed when no RFC1918 address is available and invalidates a session if its bound address disappears.
- [ ] Show concise feature-specific errors/status text; log technical detail through Task 4 diagnostics; do not disable unrelated Import/Calibrate/Measure/Arrange/Generate workflows.
- [ ] Run `python -m pytest -q tests/unit/test_capture_resilience.py tests/unit/test_ui_smoke.py`; commit `fix: keep optional capture failures recoverable`.

## Self-review result

With these amendments, every approved design area has an explicit implementation/test path: versioning, installer/portable artifacts, frozen runtime paths, self-test, diagnostics, unsaved edits, preferences, long operations, camera/network resilience, manufacturing fixture, physical validation, PR CI, tagged pre-release, and merge/tag authorization gates.
