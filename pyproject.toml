[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tooldrawer-studio"
dynamic = ["version"]
description = "Local-first tool tracing and organizer design for Windows"
requires-python = ">=3.12,<3.13"
dependencies = [
  "PySide6>=6,<7",
  "numpy>=2,<3",
  "opencv-python>=4,<5",
  "cadquery>=2,<3",
  "qrcode>=8,<9",
  "shapely>=2,<3",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
]

[tool.hatch.version]
path = "src/tooldrawer_studio/version.py"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
